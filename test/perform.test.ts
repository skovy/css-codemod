// import { perform } from '../src/perform';

describe('perform', () => {
  describe('#perform', () => {
    it('should process the files and perform transformations', () => {
      // perform();
    });
  });
});
